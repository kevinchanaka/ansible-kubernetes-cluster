---
- name: Restart containerd
  ansible.builtin.systemd:
    name: containerd
    daemon_reload: true
    state: restarted
    enabled: true

- name: Restart multipath-tools
  ansible.builtin.systemd:
    name: multipath-tools
    daemon_reload: true
    state: restarted
    enabled: true
